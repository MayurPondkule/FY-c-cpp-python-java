#include <iostream>
// #include <string>

using namespace std; 

int main()
{

    string name = "mayur";
    cout << "the name is " << name<<endl;
   // cout<<"the length of name is "<<name.length()<<endl;
    cout<<"the name is "<<name.substr(1,3);
    // cout<<"the name is"<<name;

    return 0;
}